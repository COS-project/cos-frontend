import CommunityReviewCard from '@/components/community/CommunityReviewCard';

const Review = () => {
  const num = 1;
  return (
    <main className="bg-gray0 py-[2rem] w-full px-[2rem] h-screen">
      <CommunityReviewCard />
    </main>
  );
};

export default Review;
